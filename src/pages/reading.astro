---
import BaseLayout from '../layouts/BaseLayout.astro'

import { Image } from 'astro:assets'
import { SITE_TITLE } from '../consts'
import { getCollection } from 'astro:content'

const books = await getCollection('book')
---

<BaseLayout title={`Reading List | ${SITE_TITLE}`}>
  <main class="reading">
    <section class="reading__container">
      {
        books.map((book, index) => (
          <article class="reading__item">
            <a href={book.data.ogUrl} class="reading__link" target="_blank" rel="noopener noreferrer">
              <div class="reading__cover">
                <Image src={book.data.ogImage} alt={`Cover of ${book.data.title}`} loading={index < 12 ? 'eager' : 'lazy'} width={140} height={210} referrerpolicy={'no-referrer'} />
              </div>
              <h2 class="reading__title">{book.data.title}</h2>
            </a>
          </article>
        ))
      }
    </section>
  </main>
</BaseLayout>

<style>
  * {
    box-sizing: border-box;
  }

  .reading {
    max-width: 960px;
    margin: 0 auto;
    padding: 4rem 2rem 6rem;
    min-height: calc(100vh - 200px);
  }

  .reading__container {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 3rem 2rem;
  }

  .reading__item {
    display: flex;
    flex-direction: column;
  }

  .reading__link {
    text-decoration: none;
    color: inherit;
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .reading__cover {
    width: 100%;
    aspect-ratio: 3 / 4;
    overflow: hidden;
    background: var(--z-color-gray-6);
    border: 1px solid var(--z-color-hairline-light);
  }

  .reading__cover img {
    width: 100%;
    height: 100%;
    object-fit: contain;
    display: block;
    background: var(--z-color-gray-6);
  }

  .reading__title {
    font-size: var(--z-text-lg);
    font-weight: 600;
    line-height: 1.4;
    margin: 0;
    color: var(--z-color-text);
  }

  .reading__link:hover .reading__title {
    color: var(--z-color-text-accent);
  }

  /* Responsive Design */
  @media (max-width: 768px) {
    .reading {
      padding: 2rem 1.5rem 4rem;
    }

    .reading__container {
      grid-template-columns: repeat(3, 1fr);
      gap: 2.5rem 1.5rem;
    }

    .reading__link {
      gap: 0.75rem;
    }

    .reading__title {
      font-size: var(--z-text-base);
    }
  }

  @media (max-width: 480px) {
    .reading {
      padding: 1.5rem 1rem 3rem;
    }

    .reading__container {
      grid-template-columns: repeat(2, 1fr);
      gap: 2rem 1.25rem;
    }

    .reading__link {
      gap: 0.5rem;
    }

    .reading__title {
      font-size: var(--z-text-sm);
      line-height: 1.3;
    }
  }

  /* Large screens */
  @media (min-width: 1024px) {
    .reading__container {
      grid-template-columns: repeat(5, 1fr);
      gap: 4rem 3rem;
    }

    .reading__link {
      gap: 1.25rem;
    }

    .reading__title {
      font-size: var(--z-text-xl);
    }
  }

  @media (min-width: 1280px) {
    .reading__container {
      grid-template-columns: repeat(6, 1fr);
    }
  }
</style>
